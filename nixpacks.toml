[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd saas-platform && npm install --include=dev"]

[phases.build]
cmds = ["cd saas-platform && npm run build"]

[start]
cmd = "node saas-platform/dist/index.js"
