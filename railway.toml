[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd saas-platform && node dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
