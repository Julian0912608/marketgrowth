[build]
builder = "NIXPACKS"
buildCommand = "cd saas-platform && npm install && npm run build"

[deploy]
startCommand = "cd saas-platform && node dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
